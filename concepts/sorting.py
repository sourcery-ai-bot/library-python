# Sorting can be used to sort a list in ascending or descending order

nums = [1, 5, 7, 8, 5, 4, 7, 9, 0, 7, 4, 6]

print(sum(nums))
print(max(nums))
print(min(nums))

sorted(nums, key=lambda num: num)
print(sorted)
print(sorted(nums))
